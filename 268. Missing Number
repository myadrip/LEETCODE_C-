class Solution {
public:
    int missingNumber(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        int n = 0;
        for(auto num : nums)
        {
            if(num != n)
                return n;
            n++;
        }
        
        return n;
    }
};
